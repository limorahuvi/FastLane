package models.queries.passengersCount;

/**
 * properties of a feature in the geojsons collection
 * load - passengers load between 2 points
 */
public class properties {
    public double load;
    public properties(double load) {
        this.load=load;
    }
}
